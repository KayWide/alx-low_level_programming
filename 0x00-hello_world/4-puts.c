#include <stdio.h>
/* int main is the entry point
 * puts prints the argument
 * retun (0) returns a value of 0*/

int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
